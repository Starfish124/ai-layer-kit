# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ADR-007: de gebouwde canvas hoort WEL in git — "gebouwde dist in git zodat de Python-server
# geen Node nodig heeft". De regel `dist` hierboven kwam met het Vite-sjabloon mee en sprak die
# beslissing stil tegen: 0 bestanden getrackt, dus elke verse clone had een dode /flow.
!dist
